var array = [100,200,300];
function forE( func , arr){
  if ((Array.isArray(arr)) && (typeof func === 'function') ){  
  	for(let i = 0; i < arr.length; i++) {
    		func(arr[i]);
  
    }	 
  }else console.log('Параметры не верные');
}


forE(console.log,array);
